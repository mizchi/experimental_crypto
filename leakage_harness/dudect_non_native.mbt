///| Non-native targets keep the package buildable; dudect is native-only.

///|
fn dudect_max_abs_t(
  _sparse : () -> Unit,
  _dense : () -> Unit,
  _measurements : Int,
  _rounds : Int,
) -> Double {
  -1.0
}
