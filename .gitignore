_build/
target/
.mooncakes/
*.mi

# moon prove toolchain (project-local opam switch with Why3)
.opam/

# Why3 user config written by `why3 config detect`; not part of the repo
*.why3.conf

# direnv / nix
.direnv/
result
result-*
proofs/why3.conf

# Python bytecode
__pycache__/
*.pyc

# Full-corpus FP-audit fixture (~7.6 MB) generated by scripts/audit_x509_limbo.py;
# regenerate locally, do not commit. pkix_verify/audit_reject_js_test.mbt no-ops
# when it is absent.
testdata/x509-limbo/audit_reject.json
